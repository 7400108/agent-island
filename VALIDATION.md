# 验证记录

## 1.3 按需显示（2026-09-07）

- 无通知、无运行任务时隐藏原生窗口，不占据桌面顶部；托盘及后台监听保留。
- 独立 Electron 测试覆盖：空闲启动/重启隐藏、托盘入口主动打开、任务开始自动出现、清空后隐藏、运行中清空仍显示、中断且无通知后隐藏。
- 完成后 2 秒展开、手动阅读保持、焦点保护、复制与清空、旧记录迁移的既有回归继续通过；核心测试 11 项通过。
- 显式等待异步 IPC 条件，避免测试提前通过；屏蔽隐藏后迟到的 ResizeObserver 更新，防止空胶囊重新出现。
- 打包后 `win-unpacked/Agent Island.exe` 再次通过完整桌面回归；100% / 150% / 200% 缩放、透明穿透与不抢焦点验证通过。1.3.0 安装版、便携版已生成，校验值已追加。

## 1.2 交互更新（2026-09-07）

- 12 根错峰波形：验证柱数、高低差超过 8 逻辑像素，且不同时间的柱高会变化。
- 不再区分已读未读，打开后计数不变；旧版两种状态的通知都完整保留并移除旧字段。
- 展开直接显示完整 Markdown 答案，支持复制；没有二级详情、单条删除、阅读状态标签或清空确认。
- 完成后自动展开约 2 秒再收起，另一原生测试窗口始终保持焦点；点击答案转为手动阅读后不自动关闭，新的回答不打断设置页面。
- 顶部一次点击清空全部；重复完成事件和重启不恢复已清空记录。
- 11 项核心测试、生产构建、开发与打包后 Electron 桌面回归测试全部通过，渲染错误为零。
- 100% / 150% / 200% 缩放及透明圆角穿透测试通过。便携版和安装版均已构建，SHA256 见 `release/SHA256SUMS.txt`。
- 本轮使用独立测试目录，通过 Codex 日志夹具及真实 PowerShell Claude 钩子投递验证；没有重新请求真实模型，也未改动用户通知、来源设置或全局钩子。

以下为 1.0 / 1.1 的历史验证，其中旧版阅读状态与单条删除交互已在 1.2 移除。

## 1.1 紧凑样式更新

- 在用户已打开的 OrcaTerm 中新建测试终端，实际启动 Codex 发送一句只读测试消息，观察并截图了运行态、完成后的圆形数字角标和展开的单条通知。原有终端会话未修改。
- 实测 OrcaTerm 收起窗口为 150 × 50 屏幕像素（当前 125% 缩放），展开空面板为 600 × 78；新版实现 120 × 40 和 480 × 64 逻辑像素，通知数量驱动展开高度。
- 新版已重新通过完整桌面交互测试和 100%／150%／200% 缩放测试，透明边角穿透、焦点保护、答案阅读和清理均正常。截图见 `output/playwright`，参考截图见 `output/reference`。

验证日期：2026-09-07。环境：Windows 11 x64（10.0.22635）、Node.js 24.11.1、Electron 44.2.0。

## 已通过

- TypeScript 前端、主进程和 preload 编译，Vite 生产构建。
- 10 项核心测试：JSONL 增量读取、UTF-8 半行、文件截断、新会话、交互 CLI 与 exec 来源、子智能体过滤、去重和删除后重启、并行阅读计数、超时/中断、Claude 钩子备份/恢复与延迟开始事件。
- 真实 Electron 交互：空闲胶囊、来源开关、双任务波形、两条未读、Markdown/代码块、复制、单条删除、全已读、清空、重启去重。渲染进程没有运行错误。
- 原生窗口始终置顶、主显示器居中；透明边距及圆角调用原生点击穿透；其他测试窗口保持焦点，通知不会抢焦点。
- 100%、150%、200% 设备缩放：内容无横向溢出，面板位于窗口内。
- 真实 Codex CLI 0.153.4：执行一个只回复测试标记、不调用工具的请求，CLI 正常退出，通知适配器收到完整最终回答。
- 真实 Claude Code 2.1.263：使用独立会话配置发起短请求，收到提交/停止/会话结束钩子，通知适配器收到完整最终回答。全局 Claude 配置未改动。
- 安装版与便携版构建成功；发布文件位于 `release`。
- 打包后的 `win-unpacked/Agent Island.exe` 再次通过完整桌面测试，包括真实 PowerShell 钩子投递、重启、读取和清理；不是仅在开发环境验证。

## 实测中修复

- `codex exec` 的日志来源为 `exec` / `codex_exec`，已纳入 CLI 适配。
- Claude 立即退出时可能杀掉异步 Stop 通知，结束事件改为有 3 秒上限的本地落盘钩子；运行中事件保持异步。
- 使用 Claude `prompt_id` 关联轮次，避免异步开始事件晚到导致已完成任务重新显示运行中。
- Windows 打包器解压目录重命名遇到权限错误，改为复用已安装并校验过的 Electron 分发目录。

## 尚未进行的环境测试

- 实际多显示器热插拔、硬件组合切换和 Windows 登录后的开机启动。
- 不同电脑上的 NSIS 安装/卸载、杀毒软件提示和企业策略环境。
- WSL、远程 CLI、浏览器对话、独占全屏窗口不属于首版支持范围。

可复现脚本和命令见 README；JSON 结果及截图位于 `output/playwright`。真实 CLI 检查不输出或记录凭据，测试状态和 hook 配置使用独立临时目录。
